# Git LFS for the heavy binary assets — 144 source PNGs and 6 full-res PDFs.
# Everything else stays in regular git.
rolls/**/*.png filter=lfs diff=lfs merge=lfs -text
rolls/**/*.pdf filter=lfs diff=lfs merge=lfs -text
